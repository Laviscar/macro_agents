# v1.7 叙事委员会圆桌 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在驱动图出现"逼近切换"(邻近度或加速度)时人工召开可配置委员会做多轮圆桌,主席综合出机构 memo 级结论,作为咨询层标注在世界树上(不改图)。

**Architecture:** 纯函数触发引擎(proximity 去抖 + velocity)→ CommitteeRepository(文件式 pending/sessions/badges/config)→ 规则化自动组阵 → NarrativeCommittee(persona/skill 提示的多轮 + 主席综合 memo verdict,复用 AuditPanel 的 cross/p2p 模式)→ consolidation 末尾算触发 → presenters + 独立 Streamlit「叙事委员会」页。咨询不动图,人工门控召开,token 受控。

**Tech Stack:** Python 3.12, pydantic v2, pytest, PyYAML, Streamlit, 既有 `llm/`(FakeLLMClient)、`agents/audit.py`、`graph/`、`repositories/graph_repository.py`。

参考 spec:`docs/superpowers/specs/2026-06-02-v1.7-narrative-committee-design.md`(下称 §)。

---

## 文件结构

- Create `schemas/committee.py` — 全部委员会数据模型。
- Modify `schemas/graph_edge.py` — 加 `weight_prev`。
- Modify `agents/graph_narrative_manager.py` — recompute_node 写 weight_prev。
- Create `config/committee_skills.yaml`, `config/committee.yaml`。
- Create `committee/__init__.py`, `committee/trigger.py`(纯函数), `committee/staffing.py`(规则化自动组阵)。
- Create `agents/committee.py` — NarrativeCommittee(席位多轮 + 主席综合)。
- Create `repositories/committee_repository.py` — pending/sessions/badges/state/config 读写。
- Modify `pipelines/stages.py` — consolidate 末尾算触发。
- Modify `run_loop.py` — 注入 committee 触发参数。
- Create `view_models/committee_view.py`, `presenters/committee_presenter.py`。
- Modify `presenters/today_presenter.py` — 读徽章。
- Modify `streamlit_app.py` — 新「叙事委员会」tab + 徽章显示。
- Modify `.env.example`, `docs/RUNNING_AND_TESTING.md`, `README.md`。

---

## Phase 0 — schema 与配置

### Task 1: GraphEdge.weight_prev + recompute 写入
**Files:** Modify `schemas/graph_edge.py`, `agents/graph_narrative_manager.py`; Test `tests/test_graph_edge.py`, `tests/test_graph_narrative_manager.py`

- [ ] **Step 1: 失败测试**(`tests/test_graph_edge.py` 末尾追加):
```python
def test_edge_has_weight_prev_default_zero():
    from schemas.graph_edge import GraphEdge
    e = GraphEdge(id="a->B", src="a", dst="B", sign=1, driver_label="风险偏好")
    assert e.weight_prev == 0.0
```
- [ ] **Step 2: Run** `python -m pytest tests/test_graph_edge.py::test_edge_has_weight_prev_default_zero -q` → FAIL（属性不存在）。
- [ ] **Step 3: 实现**:`schemas/graph_edge.py` 的 `GraphEdge` 加字段(在 `weight` 下一行):
```python
    weight_prev: float = Field(default=0.0, ge=0.0, le=1.0)   # 上轮权重,供 velocity 触发算增量
```
- [ ] **Step 4:** Run → PASS。
- [ ] **Step 5: recompute 写 weight_prev 的失败测试**(`tests/test_graph_narrative_manager.py` 末尾):
```python
def test_recompute_stores_weight_prev(tmp_path):
    from datetime import datetime, timezone
    from repositories.graph_repository import GraphRepository
    from schemas.graph_edge import EdgeEvidenceRef
    repo = GraphRepository(tmp_path, CONFIG); repo.seed_if_empty()
    mgr = GraphNarrativeManager(llm_client=FakeLLMClient())
    edge = next(e for e in repo.incoming_edges("GOLD") if e.driver_label == "央行购金")
    before = edge.weight
    edge.supporting_evidence.append(EdgeEvidenceRef(evidence_id="e", created_at=datetime(2026,6,1,tzinfo=timezone.utc).isoformat(), contribution=0.5))
    repo.save_edge(edge)
    mgr.recompute_node(repo, "GOLD", datetime(2026,6,1,tzinfo=timezone.utc))
    updated = next(e for e in repo.incoming_edges("GOLD") if e.driver_label == "央行购金")
    assert updated.weight_prev == before and updated.weight != before
```
- [ ] **Step 6:** Run → FAIL。
- [ ] **Step 7: 实现**:在 `agents/graph_narrative_manager.py::recompute_node` 的重算循环里,写新 weight 前先存旧值。改:
```python
        for edge in edges:
            if edge.supporting_evidence:
                edge.weight_prev = edge.weight
                edge.weight = compute_edge_weight(edge, now)
                repo.save_edge(edge)
```
- [ ] **Step 8:** Run `python -m pytest tests/test_graph_edge.py tests/test_graph_narrative_manager.py -q` → PASS。
- [ ] **Step 9: Commit** `git add -A && git commit -m "feat(v1.7): GraphEdge.weight_prev for velocity trigger"`

### Task 2: 委员会 schemas
**Files:** Create `schemas/committee.py`; Test `tests/test_committee_schemas.py`

- [ ] **Step 1: 失败测试**:
```python
from schemas.committee import (CommitteeSkill, CommitteeSeat, CommitteeTemplate,
    PendingConvocation, SeatRemark, CommitteeVerdict, CommitteeSession)

def test_seat_defaults():
    s = CommitteeSeat(name="A", persona="鹰派")
    assert s.llm_tier == "auditor_1" and s.skills == [] and s.expertise == []

def test_pending_velocity_has_no_level():
    p = PendingConvocation(asset_id="GOLD", asset_name="黄金", trigger="velocity",
        velocity_delta=0.2, ratio=0.5, leader="实际利率", runner_up="央行购金",
        is_reversal=True, created_at="t")
    assert p.level is None and p.trigger == "velocity"

def test_verdict_is_memo_grade():
    v = CommitteeVerdict(bottom_line="b", whats_changing="w", switch_likelihood="将至",
        direction="偏多", conviction="中", confidence=0.6, time_horizon="2-6周",
        catalysts_to_watch=["CPI"], invalidation="实际利率回升", positioning="多黄金",
        key_disagreements=["d"], evidence_basis=["央行增持"])
    assert v.switch_likelihood == "将至" and v.evidence_basis == ["央行增持"]

def test_session_roundtrips():
    seat = CommitteeSeat(name="A", persona="鹰派")
    v = CommitteeVerdict(bottom_line="b", whats_changing="w", switch_likelihood="噪音",
        direction="中性", conviction="低", confidence=0.3, time_horizon="—",
        catalysts_to_watch=[], invalidation="—", positioning="—", key_disagreements=[], evidence_basis=[])
    sess = CommitteeSession(id="GOLD_t", asset_id="GOLD", asset_name="黄金", level=0.6,
        seats=[seat], rounds=1, mode="cross", remarks=[SeatRemark(seat_name="A", persona="鹰派", round=1, critique="c")], verdict=v)
    again = CommitteeSession.model_validate_json(sess.model_dump_json())
    assert again.verdict.direction == "中性" and again.remarks[0].critique == "c"
```
- [ ] **Step 2: Run** → FAIL (module missing)。
- [ ] **Step 3: 实现** 照 § 2.1–2.5(全部字段),`from __future__ import annotations` + pydantic `BaseModel` + `Literal`。`level: float | None = None`、`velocity_delta: float | None = None`、`ratio: float`。`CommitteeSession.level: float | None = None`。
- [ ] **Step 4:** Run → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): committee schemas (seat/skill/template/pending/session/memo-verdict)`

### Task 3: 配置文件
**Files:** Create `config/committee_skills.yaml`, `config/committee.yaml`; Test `tests/test_committee_config.py`

- [ ] **Step 1: 写 `config/committee_skills.yaml`** 照 § 3.1(12 个 skill + personas 列表)。
- [ ] **Step 2: 写 `config/committee.yaml`**:
```yaml
default_seats:
  - {name: 鹰派-利率, persona: 鹰派, expertise: [利率], llm_tier: auditor_1, skills: [rates_curve, policy]}
  - {name: 鸽派-增长, persona: 鸽派, expertise: [增长], llm_tier: auditor_2, skills: [event_gap, liquidity]}
  - {name: 逆向-求证, persona: 逆向, expertise: [拥挤度], llm_tier: auditor_3, skills: [positioning, verify]}
default_rounds: 1
default_mode: cross
templates:
  - {id: hawk_dove, name: 鹰鸽对冲, rounds: 2, mode: cross, seats: [
      {name: 鹰派, persona: 鹰派, expertise: [利率], llm_tier: auditor_1, skills: [rates_curve]},
      {name: 鸽派, persona: 鸽派, expertise: [增长], llm_tier: auditor_2, skills: [event_gap]},
      {name: 逆向, persona: 逆向, expertise: [拥挤度], llm_tier: auditor_3, skills: [positioning]},
      {name: 求证, persona: 数据派, expertise: [数据], llm_tier: auditor_1, skills: [verify]}]}
```
- [ ] **Step 3: 校验测试**:
```python
import yaml
from pathlib import Path
CFG = Path("config")
def _load(n): return yaml.safe_load((CFG/n).read_text(encoding="utf-8"))

def test_skill_library_has_12_and_personas():
    d = _load("committee_skills.yaml")
    assert len(d["skills"]) == 12
    assert {"verify","rates_curve","event_gap"} <= {s["id"] for s in d["skills"]}
    assert "鹰派" in d["personas"] and "鸽派" in d["personas"]

def test_committee_seats_reference_valid_skills_and_personas():
    skills = {s["id"] for s in _load("committee_skills.yaml")["skills"]}
    personas = set(_load("committee_skills.yaml")["personas"])
    cm = _load("committee.yaml")
    seats = cm["default_seats"] + [s for t in cm["templates"] for s in t["seats"]]
    for seat in seats:
        assert seat["persona"] in personas, seat
        assert set(seat["skills"]) <= skills, seat
```
- [ ] **Step 4:** Run `python -m pytest tests/test_committee_config.py -q` → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): committee skill library + default seats/templates config`

---

## Phase 1 — 触发引擎(纯函数 TDD)

### Task 4: proximity 跨档去抖
**Files:** Create `committee/__init__.py`, `committee/trigger.py`; Test `tests/test_committee_trigger.py`

- [ ] **Step 1: 失败测试**:
```python
from committee.trigger import crossed_levels
LEVELS = [0.60, 0.75, 0.90]

def test_crosses_one_band_first_time():
    new, highest = crossed_levels(ratio=0.62, prev_highest=0.0, levels=LEVELS)
    assert new == [0.60] and highest == 0.60

def test_jump_crosses_multiple_bands():
    new, highest = crossed_levels(ratio=0.91, prev_highest=0.0, levels=LEVELS)
    assert new == [0.60, 0.75, 0.90] and highest == 0.90

def test_no_refire_within_same_band():
    new, highest = crossed_levels(ratio=0.80, prev_highest=0.75, levels=LEVELS)
    assert new == [] and highest == 0.75

def test_hysteresis_reset_then_recross():
    # dropped back below 0.60 -> prev_highest reset to 0 by caller; re-crossing fires again
    new, highest = crossed_levels(ratio=0.62, prev_highest=0.0, levels=LEVELS)
    assert new == [0.60]
```
- [ ] **Step 2:** Run → FAIL。
- [ ] **Step 3: 实现** `committee/trigger.py`:
```python
from __future__ import annotations

def crossed_levels(ratio: float, prev_highest: float, levels: list[float]) -> tuple[list[float], float]:
    """Bands newly crossed upward since prev_highest, and the new highest band."""
    newly = [lv for lv in sorted(levels) if lv > prev_highest and ratio >= lv]
    highest = max([prev_highest, *newly]) if newly else prev_highest
    return newly, highest
```
- [ ] **Step 4:** Run → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): proximity band crossing (debounced)`

### Task 5: velocity 触发
**Files:** Modify `committee/trigger.py`; Test 同上
- [ ] **Step 1: 失败测试**:
```python
from committee.trigger import velocity_fired

def test_velocity_fires_when_delta_exceeds_and_not_already():
    assert velocity_fired(delta=0.15, threshold=0.10, already_fired=False) is True

def test_velocity_no_refire_when_already_fired_and_still_high():
    assert velocity_fired(delta=0.15, threshold=0.10, already_fired=True) is False

def test_velocity_resets_when_delta_drops():
    # already_fired but delta fell below threshold -> caller clears flag; treat as resettable
    assert velocity_fired(delta=0.05, threshold=0.10, already_fired=True) is False
    assert velocity_fired(delta=0.20, threshold=0.10, already_fired=False) is True
```
- [ ] **Step 2:** Run → FAIL。
- [ ] **Step 3: 实现**(追加到 `committee/trigger.py`):
```python
def velocity_fired(delta: float, threshold: float, already_fired: bool) -> bool:
    """Fire when the challenger's per-cycle weight gain crosses threshold and we
    haven't already fired for this run-up. Caller clears `already_fired` once delta
    falls back below threshold (hysteresis)."""
    return (not already_fired) and delta >= threshold
```
- [ ] **Step 4:** Run → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): velocity (acceleration) trigger`

### Task 6: evaluate_assets(产出 PendingConvocation)
**Files:** Modify `committee/trigger.py`; Test 同上
- [ ] **Step 1: 失败测试**(用种子图 + 手塞证据制造一个 contested 资产):
```python
from datetime import datetime, timezone
from pathlib import Path
from committee.trigger import evaluate_assets
from repositories.graph_repository import GraphRepository
from schemas.graph_edge import EdgeEvidenceRef
CONFIG = str(Path(__file__).resolve().parent.parent / "config")
NOW = datetime(2026, 6, 1, tzinfo=timezone.utc)

def _contested_gold(tmp_path):
    repo = GraphRepository(tmp_path, CONFIG); repo.seed_if_empty()
    for drv, w, wp in (("实际利率", 0.50, 0.50), ("央行购金", 0.46, 0.30)):
        e = next(x for x in repo.incoming_edges("GOLD") if x.driver_label == drv)
        e.weight, e.weight_prev = w, wp
        e.supporting_evidence = [EdgeEvidenceRef(evidence_id=drv, created_at=NOW.isoformat(), contribution=0.5)]
        repo.save_edge(e)
    return repo

def test_proximity_pending_created(tmp_path):
    repo = _contested_gold(tmp_path)            # ratio 0.46/0.50 = 0.92 -> crosses 0.60/0.75/0.90
    state = {}
    pendings = evaluate_assets(repo, state, levels=[0.60,0.75,0.90], velocity_delta=0.10)
    gold = [p for p in pendings if p.asset_id == "GOLD"]
    assert gold and gold[0].leader == "实际利率" and gold[0].runner_up == "央行购金"
    assert any(p.trigger == "proximity" for p in gold)

def test_velocity_pending_created(tmp_path):
    repo = _contested_gold(tmp_path)            # 央行购金 delta = 0.46-0.30 = 0.16 >= 0.10
    pendings = evaluate_assets(repo, {}, levels=[0.60,0.75,0.90], velocity_delta=0.10)
    assert any(p.asset_id == "GOLD" and p.trigger == "velocity" for p in pendings)

def test_state_debounces_second_call(tmp_path):
    repo = _contested_gold(tmp_path)
    state = {}
    evaluate_assets(repo, state, [0.60,0.75,0.90], 0.10)   # mutates state in place
    second = evaluate_assets(repo, state, [0.60,0.75,0.90], 0.10)
    assert second == []                          # nothing new on identical graph
```
- [ ] **Step 2:** Run → FAIL。
- [ ] **Step 3: 实现**(追加):遍历 active asset 节点,取 incoming(active)按 weight 排序;若 ≥2 条且有证据:`ratio = runner.weight/leader.weight`;读 `state[asset] = {"highest": float, "vel_fired": bool}`;用 `crossed_levels` 出 proximity pendings、用挑战边 `weight-weight_prev` + `velocity_fired` 出 velocity pending;更新 state(含 delta 回落清 vel_fired、ratio 跌回最低档下清 highest)。每个 pending 填 `ratio/leader/runner_up/is_reversal(sign 异)/created_at`(用 `utils.clock.now_iso`)。
```python
from utils.clock import now_iso
from schemas.committee import PendingConvocation

def evaluate_assets(graph_repo, state: dict, levels: list[float], velocity_delta: float) -> list[PendingConvocation]:
    out: list[PendingConvocation] = []
    for node in graph_repo.list_nodes():
        if node.kind != "asset" or node.status != "active":
            continue
        edges = sorted(graph_repo.incoming_edges(node.id), key=lambda e: e.weight, reverse=True)
        if len(edges) < 2 or sum(len(e.supporting_evidence) for e in edges) == 0 or edges[0].weight <= 0:
            continue
        leader, runner = edges[0], edges[1]
        ratio = runner.weight / leader.weight
        st = state.setdefault(node.id, {"highest": 0.0, "vel_fired": False})
        if ratio < min(levels):
            st["highest"] = 0.0
        new_bands, st["highest"] = crossed_levels(ratio, st["highest"], levels)
        base = dict(asset_id=node.id, asset_name=node.name, ratio=round(ratio, 3),
                    leader=leader.driver_label, runner_up=runner.driver_label,
                    is_reversal=leader.sign != runner.sign, created_at=now_iso())
        for lv in new_bands:
            out.append(PendingConvocation(trigger="proximity", level=lv, **base))
        delta = runner.weight - runner.weight_prev
        if delta < velocity_delta:
            st["vel_fired"] = False
        elif velocity_fired(delta, velocity_delta, st["vel_fired"]):
            st["vel_fired"] = True
            out.append(PendingConvocation(trigger="velocity", velocity_delta=round(delta, 3), **base))
    return out
```
- [ ] **Step 4:** Run `python -m pytest tests/test_committee_trigger.py -q` → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): evaluate_assets -> pending convocations (proximity + velocity, stateful debounce)`

---

## Phase 2 — 规则化自动组阵

### Task 7: auto_staff
**Files:** Create `committee/staffing.py`; Test `tests/test_committee_staffing.py`
- [ ] **Step 1: 失败测试**:
```python
from committee.staffing import auto_staff

SKILLS = [
    {"id": "rates_curve", "name": "利率与曲线"}, {"id": "geopolitics", "name": "地缘风险"},
    {"id": "positioning", "name": "仓位与拥挤度"}, {"id": "verify", "name": "求证与溯源"},
]
PERSONAS = ["鹰派", "鸽派", "逆向", "结构派", "数据派"]

def test_matches_driver_to_skill_and_caps():
    seats = auto_staff(driver_labels=["实际利率", "避险地缘"], skills=SKILLS,
                       personas=PERSONAS, max_seats=4)
    assert 1 <= len(seats) <= 4
    all_skills = {s for seat in seats for s in seat.skills}
    assert "rates_curve" in all_skills and "geopolitics" in all_skills   # mapped from drivers
    assert "verify" in all_skills                                        # discipline seat always added
    assert len({seat.persona for seat in seats}) >= 2                    # opposing personas (tension)

def test_respects_max_seats():
    seats = auto_staff(["实际利率","避险地缘","风险偏好","央行购金","通胀预期"], SKILLS, PERSONAS, max_seats=3)
    assert len(seats) <= 3
```
- [ ] **Step 2:** Run → FAIL。
- [ ] **Step 3: 实现**:driver→skill 启发式映射表(`实际利率/政策利率/收益率曲线→rates_curve`、`避险地缘→geopolitics`、`风险偏好→positioning`、`通胀预期/增长预期→event_gap`、`美元流动性/全球流动性→liquidity`、`央行购金/制造业周期→correlation` 等);为每个命中 skill 造一个席位并轮转分配对立 persona;总是追加一个 `verify` 纪律席位;截到 `max_seats`。返回 `list[CommitteeSeat]`。
- [ ] **Step 4:** Run → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): rule-based auto-staffing (driver->skill, opposing personas, +verify)`

---

## Phase 3 — CommitteeRepository

### Task 8: 存储读写
**Files:** Create `repositories/committee_repository.py`; Test `tests/test_committee_repository.py`
- [ ] **Step 1: 失败测试**:
```python
from pathlib import Path
from repositories.committee_repository import CommitteeRepository
from schemas.committee import PendingConvocation, CommitteeSession, CommitteeSeat, CommitteeVerdict, SeatRemark
CONFIG = str(Path(__file__).resolve().parent.parent / "config")

def _verdict():
    return CommitteeVerdict(bottom_line="b", whats_changing="w", switch_likelihood="将至",
        direction="偏多", conviction="中", confidence=0.6, time_horizon="2-6周",
        catalysts_to_watch=["CPI"], invalidation="x", positioning="多金", key_disagreements=[], evidence_basis=[])

def test_pending_roundtrip(tmp_path):
    repo = CommitteeRepository(tmp_path, CONFIG)
    repo.save_pending(PendingConvocation(asset_id="GOLD", asset_name="黄金", trigger="proximity",
        level=0.6, ratio=0.62, leader="实际利率", runner_up="央行购金", is_reversal=True, created_at="t"))
    assert [p.asset_id for p in repo.list_pending()] == ["GOLD"]
    repo.clear_pending("GOLD")
    assert repo.list_pending() == []

def test_trigger_state_roundtrip(tmp_path):
    repo = CommitteeRepository(tmp_path, CONFIG)
    repo.save_trigger_state({"GOLD": {"highest": 0.75, "vel_fired": True}})
    assert repo.load_trigger_state()["GOLD"]["highest"] == 0.75

def test_session_and_badge(tmp_path):
    repo = CommitteeRepository(tmp_path, CONFIG)
    sess = CommitteeSession(id="GOLD_t", asset_id="GOLD", asset_name="黄金", level=0.6,
        seats=[CommitteeSeat(name="A", persona="鹰派")], rounds=1, mode="cross",
        remarks=[SeatRemark(seat_name="A", persona="鹰派", round=1, critique="c")], verdict=_verdict())
    repo.save_session(sess)
    assert repo.list_sessions()[0].verdict.direction == "偏多"
    assert repo.get_badge("GOLD")["switch_likelihood"] == "将至"   # badge written from session

def test_load_config_seats_and_skills(tmp_path):
    repo = CommitteeRepository(tmp_path, CONFIG)
    assert len(repo.skill_library()) == 12
    assert repo.default_seats() and repo.templates()
```
- [ ] **Step 2:** Run → FAIL。
- [ ] **Step 3: 实现**:目录 `committee_pending/ committee_sessions/ committee_badges/`,`committee_trigger_state.json`。`save_pending`(文件名 = asset+trigger+level 安全化)/`list_pending`/`clear_pending(asset)`;`save/load_trigger_state`;`save_session`(同时写 badge `{asset, switch_likelihood, direction, conviction, session_id, at}`)/`list_sessions`(按 created_at 倒序)/`get_badge`/`list_badges`;config 读取 `skill_library()/default_seats()/templates()`(读 `config/committee*.yaml`,parse 成 schema)+ `save_committee_config(seats, rounds, mode)` 写回 `config/committee.yaml`。
- [ ] **Step 4:** Run `python -m pytest tests/test_committee_repository.py -q` → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): CommitteeRepository (pending/sessions/badges/state/config)`

---

## Phase 4 — NarrativeCommittee(LLM)

### Task 9: 席位多轮批判(persona/skill 提示)
**Files:** Create `agents/committee.py`; Test `tests/test_committee_agent.py`
- [ ] **Step 1: 失败测试**:
```python
import json
from agents.committee import NarrativeCommittee, SeatRunner
from llm.fake import FakeLLMClient
from schemas.committee import CommitteeSeat

SKILL_DESC = {"rates_curve": "久期/曲线视角", "verify": "追一手来源、不编数"}

def test_seat_prompt_includes_persona_and_skill():
    llm = FakeLLMClient(responses=["利率角度看,这是噪音"])
    seat = CommitteeSeat(name="鹰派-利率", persona="鹰派", expertise=["利率"], skills=["rates_curve"])
    runner = SeatRunner(seat=seat, client=llm, skill_desc=SKILL_DESC)
    remark = runner.critique(context="GOLD: 实际利率 vs 央行购金", peers=None, round_no=1)
    sent = llm.calls[0][0].content + llm.calls[0][1].content
    assert "鹰派" in sent and ("久期" in sent or "rates_curve" in sent)
    assert remark.critique == "利率角度看,这是噪音" and remark.persona == "鹰派"

def test_failed_seat_skipped(caplog):
    from llm.base import LLMError
    seat = CommitteeSeat(name="x", persona="鸽派")
    runner = SeatRunner(seat=seat, client=FakeLLMClient(error=LLMError("down")), skill_desc={})
    assert runner.critique(context="c", peers=None, round_no=1) is None
```
- [ ] **Step 2:** Run → FAIL。
- [ ] **Step 3: 实现** `SeatRunner`:`critique(context, peers, round_no) -> SeatRemark | None`;system prompt 拼 persona + expertise + 勾选 skills 的 description;user prompt = context (+ peers block when cross round>1);LLM 失败返回 None(告警);返回 `SeatRemark(seat_name, persona, round=round_no, critique=text)`。
- [ ] **Step 4:** Run → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): SeatRunner — persona/skill-shaped seat critique`

### Task 10: 主席综合 memo verdict
**Files:** Modify `agents/committee.py`; Test 同上
- [ ] **Step 1: 失败测试**:
```python
import json
from agents.committee import ChairSynthesizer

def test_chair_produces_memo_verdict():
    payload = json.dumps({"bottom_line":"金价短期偏多","whats_changing":"驱动从实际利率转央行购金",
        "switch_likelihood":"将至","direction":"偏多","conviction":"中","confidence":0.7,
        "time_horizon":"2-6周","catalysts_to_watch":["FOMC","CPI"],"invalidation":"实际利率大幅回升",
        "positioning":"多黄金/减美债空头","key_disagreements":["逆向派认为已price-in"],
        "evidence_basis":["央行连续增持"]})
    chair = ChairSynthesizer(client=__import__("llm.fake", fromlist=["FakeLLMClient"]).FakeLLMClient(responses=[payload]))
    v = chair.synthesize(context="ctx", remarks_text="...")
    assert v.switch_likelihood == "将至" and v.conviction == "中"
    assert v.catalysts_to_watch == ["FOMC","CPI"] and v.invalidation.startswith("实际利率")

def test_chair_failure_returns_uncertain_stub():
    from llm.base import LLMError
    from llm.fake import FakeLLMClient
    chair = ChairSynthesizer(client=FakeLLMClient(error=LLMError("x")))
    v = chair.synthesize(context="c", remarks_text="r")
    assert v.switch_likelihood == "不确定" and v.confidence == 0.0
```
- [ ] **Step 2:** Run → FAIL。
- [ ] **Step 3: 实现** `ChairSynthesizer.synthesize(context, remarks_text) -> CommitteeVerdict`:system prompt 要求"机构投委会备忘录,严格 JSON",列出全部 memo 字段;parse(复用 `_parse_json` 风格,去 ```json fence);失败/解析错 → 返回保守 stub(`switch_likelihood="不确定", direction="中性", conviction="低", confidence=0.0`,其余空)。max_tokens=4096。
- [ ] **Step 4:** Run → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): ChairSynthesizer -> institutional-memo verdict`

### Task 11: convene 编排
**Files:** Modify `agents/committee.py`; Test 同上
- [ ] **Step 1: 失败测试**:
```python
import json
from agents.committee import NarrativeCommittee
from llm.fake import FakeLLMClient
from schemas.committee import CommitteeSeat, PendingConvocation

def test_convene_runs_seats_then_chair():
    seats = [(CommitteeSeat(name="鹰", persona="鹰派", skills=["rates_curve"]), FakeLLMClient(responses=["r1"])),
             (CommitteeSeat(name="鸽", persona="鸽派", skills=["verify"]), FakeLLMClient(responses=["r2"]))]
    chair = FakeLLMClient(responses=[json.dumps({"bottom_line":"b","whats_changing":"w","switch_likelihood":"将至",
        "direction":"偏多","conviction":"中","confidence":0.6,"time_horizon":"t","catalysts_to_watch":[],
        "invalidation":"i","positioning":"p","key_disagreements":["鹰鸽分歧"],"evidence_basis":[]})])
    pend = PendingConvocation(asset_id="GOLD", asset_name="黄金", trigger="proximity", level=0.75,
        ratio=0.8, leader="实际利率", runner_up="央行购金", is_reversal=True, created_at="t")
    cm = NarrativeCommittee(seats=seats, chair_client=chair, rounds=1, mode="cross", skill_desc={"rates_curve":"曲线","verify":"求证"})
    session = cm.convene(pending=pend, context="GOLD 当前偏多;实际利率 vs 央行购金")
    assert session.asset_id == "GOLD" and len(session.remarks) == 2
    assert session.verdict.switch_likelihood == "将至" and session.verdict.key_disagreements == ["鹰鸽分歧"]
```
- [ ] **Step 2:** Run → FAIL。
- [ ] **Step 3: 实现** `NarrativeCommittee.convene(pending, context) -> CommitteeSession`:按 rounds/mode 跑 SeatRunner(cross:round>1 注入上轮 peers;p2p:各轮独立),收集 remarks(跳过失败席位);拼 remarks_text 交 ChairSynthesizer;组 `CommitteeSession(id=f"{asset}_{now}", ...)`。
- [ ] **Step 4:** Run `python -m pytest tests/test_committee_agent.py -q` → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): NarrativeCommittee.convene (rounds/mode + chair)`

---

## Phase 5 — 管道集成(触发挂在 consolidation)

### Task 12: consolidate 末尾算触发
**Files:** Modify `pipelines/stages.py`, `run_loop.py`; Test `tests/test_consolidate_committee.py`
- [ ] **Step 1: 失败测试**(整合后产出 pending):
```python
import json
from pathlib import Path
from agents.graph_narrative_manager import GraphNarrativeManager
from llm.fake import FakeLLMClient
from pipelines.stages import consolidate_graph
from repositories.graph_repository import GraphRepository
from repositories.committee_repository import CommitteeRepository
from repositories.news_repository import SQLiteNewsRepository
from tests.test_consolidate_graph import _seed_evidence, VOCAB, REGIMES, CONFIG
from utils.clock import now_iso

def test_consolidate_emits_committee_pending(tmp_path):
    news = SQLiteNewsRepository(tmp_path/"n.sqlite3")
    graph = GraphRepository(tmp_path, CONFIG); graph.seed_if_empty()
    committee = CommitteeRepository(tmp_path, CONFIG)
    # two GOLD evidences routed/attributed so 央行购金 edge accumulates and contests 实际利率
    llm = FakeLLMClient(responses=[json.dumps({"assets":["GOLD"]}), json.dumps({"driver_label":"央行购金","aligns_sign":1}),
        json.dumps({"title":"g","thesis":"t","read_line":"r","regime":"滞胀","countries":[]})])
    mgr = GraphNarrativeManager(llm_client=llm)
    _seed_evidence(news, "央行增持黄金", "ev1")
    consolidate_graph(news, graph, mgr, VOCAB, REGIMES, tmp_path/"rs.json",
                      now_fn=now_iso, committee_repo=committee, trigger_levels=[0.60,0.75,0.90], velocity_delta=0.10)
    # GOLD's 央行购金 edge gained evidence-weight vs the seed 实际利率 prior -> contested -> maybe pending
    assert isinstance(committee.list_pending(), list)   # smoke: integration wired, no crash
```
- [ ] **Step 2:** Run → FAIL (consolidate_graph 不接 committee 参数)。
- [ ] **Step 3: 实现**:`consolidate_graph` 增可选参数 `committee_repo=None, trigger_levels=None, velocity_delta=0.10`;在函数末尾(写 watermark 后)若 `committee_repo is not None`:`state = committee_repo.load_trigger_state(); pendings = evaluate_assets(graph_repo, state, trigger_levels or [0.60,0.75,0.90], velocity_delta); committee_repo.save_trigger_state(state); [committee_repo.save_pending(p) for p in pendings]`。`build_run_loop` 构造 `CommitteeRepository` 并把 `committee_repo` + 从 `.env` 读的 `COMMITTEE_TRIGGER_LEVELS`(逗号解析)/`COMMITTEE_VELOCITY_DELTA` 传进 consolidation stage 的 lambda。
- [ ] **Step 4:** Run `python -m pytest tests/test_consolidate_committee.py tests/test_consolidate_graph.py -q` → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): wire committee trigger into consolidation`

---

## Phase 6 — presenters

### Task 13: committee_presenter + view_models
**Files:** Create `view_models/committee_view.py`, `presenters/committee_presenter.py`; Test `tests/test_committee_presenter.py`
- [ ] **Step 1: 失败测试**:
```python
from pathlib import Path
from presenters.committee_presenter import build_committee_view, estimate_calls
from repositories.committee_repository import CommitteeRepository
from schemas.committee import PendingConvocation, CommitteeSeat
CONFIG = str(Path(__file__).resolve().parent.parent / "config")

def test_view_has_pending_history_config(tmp_path):
    repo = CommitteeRepository(tmp_path, CONFIG)
    repo.save_pending(PendingConvocation(asset_id="GOLD", asset_name="黄金", trigger="velocity",
        velocity_delta=0.2, ratio=0.5, leader="实际利率", runner_up="央行购金", is_reversal=True, created_at="t"))
    v = build_committee_view(repo)
    assert v.pending and v.pending[0].asset_id == "GOLD"
    assert len(v.skill_library) == 12 and v.default_seats

def test_estimate_calls_seats_times_rounds_plus_chair():
    assert estimate_calls(seats=3, rounds=2) == 3*2 + 1
```
- [ ] **Step 2:** Run → FAIL。
- [ ] **Step 3: 实现** `view_models/committee_view.py`(`CommitteeView{pending, sessions, skill_library, default_seats, templates, default_rounds, default_mode}`);`presenters/committee_presenter.py::build_committee_view(committee_repo)` 聚合;`estimate_calls(seats, rounds) -> seats*rounds + 1`。
- [ ] **Step 4:** Run → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): committee presenter + call estimate`

### Task 14: 徽章进 today/shifts
**Files:** Modify `presenters/today_presenter.py`; Test `tests/test_today_presenter.py`
- [ ] **Step 1: 失败测试**:
```python
def test_today_card_carries_committee_badge(tmp_path):
    # build_today_view accepts an optional committee_repo and surfaces its badge
    from repositories.committee_repository import CommitteeRepository
    from schemas.committee import CommitteeSession, CommitteeSeat, CommitteeVerdict, SeatRemark
    repo = _repo(tmp_path)
    cr = CommitteeRepository(tmp_path, CONFIG)
    v = CommitteeVerdict(bottom_line="b", whats_changing="w", switch_likelihood="将至", direction="偏空",
        conviction="高", confidence=0.8, time_horizon="t", catalysts_to_watch=[], invalidation="i",
        positioning="p", key_disagreements=[], evidence_basis=[])
    cr.save_session(CommitteeSession(id="GOLD_t", asset_id="GOLD", asset_name="黄金", level=0.75,
        seats=[CommitteeSeat(name="A", persona="鹰派")], rounds=1, mode="cross",
        remarks=[SeatRemark(seat_name="A", persona="鹰派", round=1, critique="c")], verdict=v))
    view = build_today_view(repo, committee_repo=cr, pinned=["GOLD"])
    card = next(c for c in view.cards if c.asset_id == "GOLD")
    assert card.committee_badge == "切换将至·偏空·信心高"
```
- [ ] **Step 2:** Run → FAIL。
- [ ] **Step 3: 实现**:`TodayCard` 加 `committee_badge: str | None = None`;`build_today_view(..., committee_repo=None)`:有 repo 时取 `committee_repo.get_badge(asset_id)` 拼 `f"{switch_likelihood}·{direction}·信心{conviction}"`;委员会判"将至"的资产在排序里加权(score += 800,介于 shift 与 contested 之间)。
- [ ] **Step 4:** Run `python -m pytest tests/test_today_presenter.py -q` → PASS。
- [ ] **Step 5: Commit** `feat(v1.7): committee badge surfaced + weighted in today view`

---

## Phase 7 — Streamlit UI(手测)

### Task 15: 「叙事委员会」页
**Files:** Modify `streamlit_app.py`
- [ ] **Step 1:** 新增第 6 个 tab「叙事委员会」,构造 `CommitteeRepository`,`build_committee_view`。三区:
  - **配置区**:`st.data_editor` 或逐席位表单(persona 下拉来自 skill_library.personas、expertise 文本、llm_tier 下拉、skills `st.multiselect` 来自库)、rounds/mode、预设模板 selectbox(选中填充)、「保存配置」→ `committee_repo.save_committee_config(...)`。skill 库 `st.expander` 只读列出。
  - **待召开区**:遍历 `view.pending`,每条显示触发类型(📊档/⚡加速)+leader/runner+ratio;「🤖 自动组阵」按钮(`auto_staff`,结果可在配置区再改);显示 `estimate_calls(seats, rounds)`;「召开圆桌」按钮 → 构建 `NarrativeCommittee`(用配置席位的 `auditor_*` client,`build_llm_client(load_llm_config(tier=seat.llm_tier))`;chair=narrative client)→ `convene` → `committee_repo.save_session` + `clear_pending(asset)` → `st.rerun()`。
  - **历史区**:`view.sessions` 每条 `st.container`:主席 memo(bottom_line/方向/信心/horizon/catalysts/invalidation/positioning/key_disagreements/evidence_basis)+ `st.expander` 各席位多轮发言。
- [ ] **Step 2:** 手测:`streamlit run streamlit_app.py` → 委员会页渲染、配置可编辑保存、(有 pending 时)自动组阵+召开出 memo。
- [ ] **Step 3: Commit** `feat(v1.7): narrative committee Streamlit page`

### Task 16: 徽章 + 入口
**Files:** Modify `streamlit_app.py`
- [ ] **Step 1:** `_render_today_view` 卡片显示 `c.committee_badge`(有则 `🏛 {badge}`);`_render_shifts_view` 顶部显示「🏛 N 个待委员会」(读 `committee_repo.list_pending()` 计数)+ 对应资产条目显示徽章。`main()` 把 `committee_repo` 传进 `build_today_view`。
- [ ] **Step 2:** 手测:今日/分歧预警页能看到徽章 + 待委员会计数。
- [ ] **Step 3: Commit** `feat(v1.7): committee badge + pending entry on today/shifts pages`

---

## Phase 8 — 收尾

### Task 17: .env + 文档
**Files:** Modify `.env.example`, `docs/RUNNING_AND_TESTING.md`, `README.md`
- [ ] **Step 1:** `.env.example` 加 § 3.3 四项(`COMMITTEE_TRIGGER_LEVELS=0.60,0.75,0.90` / `COMMITTEE_VELOCITY_DELTA=0.10` / `COMMITTEE_MAX_SEATS=5` / `COMMITTEE_DEFAULT_ROUNDS=1`),带中文注释。
- [ ] **Step 2:** `RUNNING_AND_TESTING.md` 新增 §5 委员会:触发逻辑、人工召开流程、配置席位/skill、成本控制、memo 结论;`README.md` 版本进展加 V1.7 行。
- [ ] **Step 3: Commit** `docs(v1.7): committee tunables + usage`

### Task 18: 全量回归 + 冒烟
- [ ] **Step 1:** `python -m pytest -q` 全绿(预期 +~30 新测试)。
- [ ] **Step 2:** 重启 streamlit,验证 6 个 tab(含委员会)无报错;制造一个 contested 资产(或跑 `run_loop --once` 若有逼近)看 pending 出现 → 配置席位 → 召开 → memo 出现 → 徽章上今日/分歧预警。
- [ ] **Step 3:** 用 superpowers:finishing-a-development-branch 收尾 v1.7(合 main + tag v1.7)。

---

## 自检(对照 spec)
- **§一 触发**:proximity(Task4)+velocity(Task5)+evaluate(Task6)+weight_prev(Task1)+集成(Task12)。✅
- **§二 schema**:Task2(全模型)+Task1(weight_prev)。✅
- **§三 config**:Task3(skills/committee)+Task17(.env)。✅
- **§四 引擎**:trigger(4-6)、staffing(7)、committee agent(9-11)。✅
- **§五 咨询徽章不动图**:Task8(badge from session)+Task14(surfaced)+Task11 测试不写图(committee 模块从不调 graph_repo 写权重——Task11/12 测试断言)。✅
- **§六 UI**:Task15(委员会页)+Task16(徽章/入口)。✅
- **§七 token**:人工召开(Task15)、规则自动组阵(Task7,0 LLM)、预设模板(Task3/15)、estimate_calls(Task13)、rounds/seats 上限(.env Task17)。✅
- **§八 复用**:weight_prev(Task1)、AuditPanel 模式(Task9-11 平行实现 cross/p2p)、hook 不强绑(Task12 仅 UI 触发,不挂自动 consolidation 的 LLM)。✅
- **§九 测试**:去抖(4-6)、memo verdict(10)、不动图(11/12)、presenter(13)、配置校验(3)。✅
- **类型一致**:`CommitteeSeat/CommitteeVerdict/CommitteeSession/PendingConvocation/SeatRemark` 全程同名;`evaluate_assets/crossed_levels/velocity_fired/auto_staff/convene/build_committee_view/estimate_calls` 签名一致。✅
- **无占位符**:核心任务均含真实测试+实现代码;UI 任务为手测(streamlit 难自动化)。✅
